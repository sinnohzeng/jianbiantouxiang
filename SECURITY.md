# 安全策略

## 支持的版本

| 版本 | 支持状态 |
|------|----------|
| 3.2.x | ✅ 安全更新 |
| 3.1.x 及更早（含 v2 各标签） | 遗留版本，不再安全更新；旧渲染路线切 `v2.3.0` 标签 |

## 报告漏洞

如果您发现安全漏洞，请通过 GitHub Issues 以 **私密方式** 报告（使用 "Report a vulnerability" 功能）。

请勿在公开 Issue 中披露安全漏洞的细节。

我们会在 48 小时内确认收到报告，并在 7 天内提供修复方案或缓解建议。

## 安全实践

v3 是纯前端静态站，安全面集中在浏览器内、字体加载链与上传图形：

- 无服务端代码、无账号、无遥测；文字、配色与配置只存在用户自己的 localStorage 与用户主动分享的 URL 里，不收集、不上传任何用户数据。
- 渲染与导出全部在浏览器内完成，导出产物不经过任何第三方。
- 外部请求包括字体样式表与字体切片，以及选中 emoji 时从 jsDelivr 取 Noto Emoji SVG：Google Fonts css2 主链、fontsource 两级镜像与 Noto Emoji 都见相关文档；页面不注入任何第三方脚本。
- 上传字体经 `FontFace` 只在本会话注册；上传 SVG 图形先经白名单消毒再注册，PNG 与 WebP 原样读取。两者都不写盘、不进分享链接。
- 依赖均为知名开源库，锁文件入库，CI 每次 push 跑 lint、typecheck、单测与构建。
