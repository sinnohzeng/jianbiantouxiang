# 变更日志

所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [5.2.0] - 2026-09-05

### 新增

- **赞赏区上线**：关于页的「支持一下」接上微信与支付宝收款码。码是从收款海报里裁出来的，只留码本身加一圈重画的纯白静区，裁完用 jsQR 把原图与成品各解一遍、比对 payload 逐字一致才落盘，没有重新生成过二维码。收款码竖排展示，尺寸给到能真扫得出来，深色主题下压在白底上；两张图不进 PWA 预缓存，不让每个装应用的人先下 200 KB

### 变更

- **滚动条统一收细，轨道透明**：原生粗滚动条最扎眼的不是宽度，是那条从头贯到底的不透明轨道，卡片全是圆角玻璃质感，旁边杵一根实心灰柱子，整页被切成几块。现在轨道透明、滑块细且安静，指到哪一栏、焦点落在哪一栏，哪一栏的滑块才提上来。只用标准属性 `scrollbar-width` / `scrollbar-color`，不写 `::-webkit-scrollbar`：Chromium 里只要 `scrollbar-width` 不是 `auto`，webkit 那套伪元素整个作废，两套写一起必有一套是死代码

### 修复

- **`/about` 线上打不开，报 `ERR_TOO_MANY_REDIRECTS`**：`_redirects` 里那条 `/about  /about.html  200` 是多余且有害的。Cloudflare Pages 的静态资源层自带 HTML 规范化，`/about.html` 一定 308 到 `/about`，这条规范化对重写之后的内部路径同样生效，于是重写与规范化互相咬成死循环。删掉这条即可：静态资源在 `_redirects` 的通配兜底之前解析，`/about` 由资源层自己映射到 `about.html`

### 验证

- 单测 641 条全绿；`npm run e2e` 33 条全绿（赞赏区那条从「没配就不出现」改成「按配置渲染且图真的取到了」，断言 `naturalWidth`）
- `npm run lint`、`npm run typecheck`、`npm run build` 全绿
- `/about` 的死循环与修复都用 `wrangler pages dev dist` 在本地逐条复现并确认，跑的是与线上同一套资源层逻辑
- 滚动条加 `--disable-features=OverlayScrollbar` 截图核对：本机 Chromium 默认走 macOS 悬浮滚动条，静息态什么都不画，不强制经典滚动条就看不到实际效果
- 关于页赞赏区深浅两色与手机档逐张看图核对

## [5.1.0] - 2026-09-05

### 新增

- **独立的关于页**：`/about` 是一张真实的静态页，不是浮层。讲项目、讲怎么用、讲渲染排版字体导出各自是怎么做的，还有隐私、技术栈与英文摘要。它只有自己那一小段脚本，不拉工具那份 chunk，也能单独分享一个链接出去。站点因此变成两个入口的多页构建，`appType` 从 spa 改成 mpa，service worker 的导航兜底关掉，Pages 的 `_redirects` 里 `/about` 排在通配兜底之前
- **赞赏入口的骨架**：关于页留了一块「支持一下」，链接与收款码写在 `src/about/support.ts`，填了哪一项出哪一项，全空整块不渲染。代码里只有链接与图片路径，没有任何密钥

### 变更

- **产品名收成「渐变头像」**：去掉「生成器」三个字。界面标题、五份字典、页面 title 与 og、README 标题一起改
- **手机上不显示产品名**：顶栏那点宽度分给六个按钮之后剩不下几十像素，名字会被截成半个字。用 `sr-only` 收起而不是 `hidden`，读屏与文档大纲里这条 h1 还在
- **操作条五个按钮一律带字**：两个随机原先在窄一档会收成光秃秃的图标，得点一次才知道是干嘛的。现在宽的时候给全称（随机颜色 / 全部随机），窄下去换两三个字的短称（随机 / 全随机），没有只剩图标那一档
- **「随机配色与质感」改叫「全部随机」**：七个字放不进一个按钮，完整说明留在悬停提示里
- **文字样式的「胶囊底」改叫「胶囊」**：与另外四个两字标签齐平，窄列里不再被截成「胶…」；分段控件的水平内边距从 8 收到 6，五格控件在窄列里多出十来像素

### 修复

- **挑选栏与微调没到底**：操作条只占画框那一列之后，左边几列还停在第一行末尾，底下空出一条与操作条等高的带子，白白少掉五六十像素可视高度。改成左边几列跨满两行，只有画框那一列分上下
- **操作条两个随机按钮不显示文案**：长短两版文案里，长的那版挂了 Tailwind 的 `hidden`，工具层排在组件层之后，把容器查询里的 `display: block` 压掉了，结果两版都不显示。露不露现在只由 `index.css` 一处定，span 上不挂任何 display 工具类
- **导出抽屉的动作按钮贴着上一个控件**：上面是滚动区，内容会一直滑到按钮底下，而 `DrawerFooter` 默认 `pt-0`。补上一条分隔线与上边距，它现在读作一条固定的动作条
- **文字颜色的第七个色块孤零零折行**：七个 44 见方的色块在挑选栏那一列放不下，末尾会掉下去一个。色块改成按行等分，高度仍是 44

### 移除

- **关于对话框**：`src/app/AboutDialog.tsx` 与 `about.intro`、`about.license`、`about.version`、`topbar.github` 四条词条；顶栏那枚 ⓘ 现在是指向 `/about` 的链接。五份字典共 259 条

### 验证

- 单测 641 条全绿；`npm run e2e` 33 条全绿（新增关于页独立成页与赞赏区空态两条）
- `npm run lint`、`npm run typecheck`、`npm run build` 全绿；1440 与 1920 两档、微调开合两态、手机档、导出抽屉与关于页逐张看图核对

## [5.0.0] - 2026-09-05

工作台与债务清理，规约见 `specs/v5.0-workspace/`。

### 新增

- **炫技层**：极光着色器背景接当前配色，首屏加载动画，挑选栏与微调 stagger 进场，预览框缩放模糊入场与桌面指针跟随的 3D 倾斜高光，分段控件与磁贴的选中描边共享元素滑动，随机与导出成功时的涟漪加预览弹动，顶栏标题逐字入场，数值框平滑计数；`prefers-reduced-motion: reduce` 与 `VITE_SHOWCASE=0` 时整套不挂，回落到原来的 CSS 光晕
- **手机长按直接存图**：触屏上预览里盖着一张按当前配置合成的 JPG，长按它就走系统的保存图片，不用先点导出；预览停稳 600 毫秒后在后台出图，页面切到后台不出，网格与安全区参考线不进图
- **双列挑选栏**：页签与手风琴取消，左列文字与图标、右列配色与质感，两件最常用的事同屏并排；拆不拆两列看的是还剩多少宽度给画框：微调收起时 1280 就拆，展开时 1536 才拆，不够就并回一列
- **微调面板默认收起**：数值滑杆是低频动作，宽度先让给改文字与换配色，需要时从操作条打开，桌面在最左侧多开一列而不是浮在预览上（数值微调是“挑”的深一层，跟左边那两列同源，右边那一列是作品本身）；开合状态落本机，手机与桌面共用同一个开关
- **图形水平补偿**：图形除了大小还能左右挪，按安全框宽度的比例存，范围正负 25%，与逐行文字补偿同一口径
- **一键恢复默认**：从关于对话框挪到操作条的“更多”菜单，带一次确认；它走撤销栈，确认之后还能撤销回去
- **新版本自动发现**：service worker 每 15 分钟查一次更新，回到前台与窗口聚焦时再各查一次，有新版本弹一条带「刷新」的提示，标签页开着不关也能拿到新部署
- **滑杆带数值框与重置钮**：每根滑杆右侧常驻数字输入，回车或失焦按步进对齐并夹回区间；偏离默认值时行内出现重置小钮，桌面悬停显形、触控常显
- **手机预览可拖拽调高**：预览默认 28svh，预览与内容之间的分隔条支持拖拽（20 到 60svh）、双击回默认、上下键每次 4svh，高度存本机
- **内置品牌图形**：图标选择器加“品牌”页，58 个品牌（dashboard-icons 加 owner 提供的飞书、豆包工作、Qoder、WorkBuddy），原色与单白两种变体，按类别分组，中英文名与别名都能搜；资源是同源静态文件加懒 chunk，不进首屏；清单 `scripts/brand-list.json`，`npm run gen:brand` 生成
- **网格参考线**：正方形格子取画布短边的 1/12 从中心铺开，中心十字加粗，白色低透明度加 difference 混合，深浅底都可辨；只在预览显示，不进导出；开关状态存 localStorage（`gradient-avatar:overlays`）
- **深色系配色补齐**：新增午夜藏蓝、深邃翡翠、余烬暮色、牛血红、夜紫、深海青、玫瑰午夜、宇宙靛蓝、森林夜、铜暮、钢青灰共 11 组，浅色 21 组、深色 16 组，一共 37 组
- **更多菜单塞满**：复制图片、网格参考线、安全区参考线、恢复默认设置四项收在一处；此前它只有一项，为一项开一个菜单不值当
- **字号自动与手动无缝**：字号滑杆常驻可用，自动态显示预览刚求出的字号（按滑杆步进向下对齐，轻触不会越过求解上限），一拖就以它为起点切成手动，画面不跳；旁边「自动」按钮一键回到自动；「字号模式」分段控件删除

### 修复

- **悬停时按钮文字变深黑**：Tailwind v4 把 `peer-hover` 排在 `peer-checked` 之后，同权重下悬停规则会把选中格的文字改回 `foreground`，压在实心主色底板上就是一片看不清的深色，深浅两套主题都中招；选择器里自己带上 `:not(:checked)`，提亮只给未选中的格子
- **点“随机颜色”配色那一列会变宽**：这一列的内容高度随筛选跨过溢出阈值，滚动条一出一进就是十几像素的宽度跳变；挑选栏与微调几列统一 `scrollbar-gutter: stable`
- **第二行默认值“效率先锋”换行**：适配判据只要求第一行不折行，二分就一路把字号涨到第二行折了才停；改成两行都必须是单行才算合格，字数多的时候在边距范围内自动变小
- **预览卡片底下的分界线**：方形卡片边缘加一条短投影，在环境光上收出一道硬边，看着像容器没收住；改成取当前配色的一团大半径光晕垫在画框底下，投影换成贴边、厚度、长拖尾三层
- **随机按钮的方形色散**：随机与导出成功时的星爆是着色器铺满自己那块方形画布，摆在一百多像素的按钮上边界一览无余，点一下像一团方形色块炸开；换成纯 CSS 的一圈涟漪，按钮再小也不露边界，也不必为一次反馈起 WebGL 上下文
- **主预览区多出一条滚动条**：预览这一格改成 `overflow-hidden`，画框本来就按净空夹过一次，再挂一条滚动条只会出现在没人需要它的地方
- **进场幕布挡住点击**：幕布抽走要放完整段动画，期间界面已经露出来了，读秒一结束就停止吃指针事件
- **微信里长按保存失败**：微信内置浏览器不允许保存 `blob:` 地址的图片，导出结果改成 data URL 画成 img；微信里固定生成 JPG（安卓微信对 PNG 的 base64 长按保存不稳定），格式选择在微信里不显示
- **SVG 样式填色丢失**：净化器放行 `style` 属性里的上色声明（属性与取值双白名单），Figma 导出与 dashboard-icons 这类把填色写在 style 里的 SVG 不再变成纯黑；生成脚本落盘前把 `<style>` 类规则内联
- **行级补偿彻底独立**：补偿不再参与求解器（此前按补偿预留宽度余量，自动档里两行共用的基准字号会跟着缩，手动档里贴满的第一行会被挤到折行），换行与二分按完整安全区算，落位纯位移；位移后越界只反映在「超出安全区」提示里，不缩字号；补上自动档回归用例

### 移除

- **自动文字色与自动底板**：文字色只剩用户挑的那一个，预览与导出读同一个字段，不再有第二条判定路径；随之删掉预览链路上那个离屏 WebGL 取样探针、`text/effective.ts`、`text/auto-color.ts` 与底板徽章
- **环境光滑杆**：默认全开到最大，不再让人自己调
- **操作条的“文字”快捷入口**：两行输入常驻在挑选栏第一节，一眼就看得见，再给它一个入口是重复
- **配色的家族下拉与重复的随机按钮**：下拉只筛不选，与磁贴列表功能重叠；随机颜色与随机质感在操作条已经是一级按钮，配色那一节不必再摆一遍
- **URL 配置分享**：删除「复制链接」按钮、`#c=` 编解码、坏链接提示与 `history.replaceState` 同步；初始配置只剩本机存档与默认两档，地址栏不再挂配置载荷；`export/share.ts` 里没有调用方的 Web Share 代码一并删除
- 相关词条 `bottombar.copyLink`、`common.copied`、`share.invalid`、`panel.text.sizeMode.*`；`common.copyFailed` 改为通用「复制失败」
- 相关词条 `bottombar.text`、`panel.palette.family*`、`panel.text.color.auto*`、`preview.plate.*`、`theme.ambient`；五套词典共 263 条

### 变更

- **操作条只占预览那一列**：挑选栏底下压一条通栏的操作条，会让人以为它管的是左边那两列，而它管的其实是画面
- **操作条按钮全部带可见文案**：只有图标时没人认得出哪个是哪个，触控目标再大也没用；手机上是图标在上、短文案在下，桌面是图标加文案的一行
- **文案收放按容器宽度而不是视口**：操作条只有预览那一列宽，微调一开合就换一个量级，视口断点在这里给不出答案；改用容器查询，窄下去按分量倒序收，先收微调与更多，再收两个随机，最后才轮到导出，每个按钮都有常驻 `aria-label` 兜住可访问名
- **画布并入导出设置**：画布本来就是“导出成多大的一张图”，跟格式与体积是同一件事；默认值从 1024×1024 升到 2048×2048，体积档从 1 MB 升到 2 MB
- **文字样式默认投影，且排在第一**：默认值理应是列表里第一个，用户看到的顺序就是分量顺序
- **图形改叫图标**：大部分人一上来不知道“图形”指什么；面板文案连同五套词典一起改写，加一句说明这里放的是徽章上那枚小图
- **跟随系统换图标**：显示器画的是设备，读出来像“屏幕设置”，这一档要说的是“深浅由系统定”；换成一枚半明半暗的圆，GitHub、Notion、Raycast 都是这一路
- **选中态换成实心主色**：分段控件的白底板压在浅灰槽上只差一点点亮度，一眼看不出选的是哪一格；配色磁贴的选中描边同步加粗到 3 像素并去掉透明度
- **最近生成挪到顶栏右上角**：与撤销重做放在一起，三个都是“回到刚才那一版”；缩略图条收在浮层里，点开过一次才拉那份 chunk
- 首屏 JS 预算不再是闸门与 CI 检查项，`npm run budget` 只报体积并把 250 KB 当参考线；视觉效果优先于体积，首开慢由加载动画承担
- README、architecture、contributing、AGENTS.md、SECURITY.md 与项目记忆同步：分享链接相关描述删除，端到端与截图喂配置改为往 localStorage 写存档；补上 `budget`、`samples`、`gen:app-icons` 三条命令，删掉 v4 已退役的竖排描述
- 重复的 `export.done` 词条去重

### 验证

- 单测 641 条全绿；`npm run e2e` 32 条全绿（新增双列工作台与微调默认收起、预览区裁而不滚、更多菜单里的恢复默认与网格留存、字号自动态切手动、存档刷新恢复）
- 桌面栅格按 1280 / 1366 / 1440 / 1536 / 1600 / 1920 / 2560 七档实测：微调收起时挑选栏 340 到 400、画框 536 到 890；微调展开时 1536 以下并回一列、画框 596 到 776，1536 起拆两列、1920 上是 360 / 360 / 340 加画框 780；七档都没有溢出，操作条一处文案都没被截断
- `npm run lint`、`npm run typecheck`、`npm run build` 全绿；`npm run budget` 只报数不设闸

## [4.0.1] - 2026-09-02

### 修复

- **导出连点防护**：导出按钮加同步锁与三态（待命 / 导出中 / 已导出），loading 至少展示 600 ms，成功后 400 ms 确认态再解锁，连点窗口约一秒只出一张；同一帧两次点击的闭包竞态用 ref 锁堵上；失败立即解锁可重试，微信长按保存分支不占锁

### 变更

- **收尾约定**：contributing 写明站点在 Cloudflare Pages，main 分支 Push 即部署，改完 commit 加 push 一条龙，只 commit 不 push 不算收尾

## [4.0.0] - 2026-09-02

两行徽章模型：三个用途收敛为一个纵向栈，规约见 `specs/v4.0-two-line-badge/`。

### 新增

- **两行模型**：只支持第一行、第二行；第二行为空只渲染第一行并自动放大；第一行为空、第二行有内容是合法槽位，行级参数跟着内容走
- **图标进栈**：文字面板常驻图标开关，打开即拉起图形选择器；图标置顶，文字两行按比例缩小整体适配；清除按钮一键回纯文字
- **常驻操作条**：“随机颜色”（种子）与“随机质感与配色”从下拉二级提为一级按钮，新增“文字”快捷入口，一步切页签并聚焦第一行；窄屏退成纯图标
- **预设文字色**：白、黑、米白、明黄四档色块加自选
- **导出文件名加秒级时间戳**：`文字_宽x高_YYYYMMDD-HHmmss.扩展名`，设备本地时区，连导多张不互相覆盖

### 变更

- **默认文字效果**：发光 50% 改为投影 40%；投影颜色随文字色反色适配，深字用白漫射，补上黑字黑阴影不可见的空洞；自动取色加投影的组合也保留胶囊底兜底
- **契约升 v4**：`layout.kind`、对齐、锚点、全局偏移、竖排、自动换行开关退役，旧载荷读进来即忽略；`normalizeConfig` 承担迁移，三行以上文字第三行起并入第二行；分享链接同时接受 v3 与 v4 载荷，本地存档不作废
- **水平补偿解耦**：补偿改为落位纯位移，改第一行不再推着第二行动（v3 的 minOffset 归一化会往左移时把第二行推走）；回归用例盯死“改第 i 行其余行像素不动”

### 移除

- 用途选择器（纯文字 / 状态徽章 / 图标徽章）与自由多行文本框；三行以上的旧链接迁移为两行

### 验证

- 单测 634 条全绿：新增栈求解、补偿独立性回归、两行切段、v3 迁移与文件名时间戳用例
- `npx playwright test` 22 条全绿：图标开关、两行输入、时间戳文件名、常驻按钮一步聚焦
- `npm run lint`、`npm run typecheck`、`npm run build` 全绿

## [3.3.0] - 2026-09-01

### 新增

- **关于对话框**：顶栏源码图标改为“关于”，点开看简介、版本号、源码链接与许可；版本号构建期自 package.json 的 version 注入，跟发布不脱节
- **恢复默认设置**：放在关于对话框里；reset 走撤销栈，恢复之后还能 undo 回去，按可逆动作的惯例不上确认弹窗
- **环境光滑杆**：主题菜单加强度滑杆，0 到强可调，落 localStorage，不进分享链接与存档
- **图标位图脚本**：`npm run gen:app-icons` 用本机 chromium 把 SVG 图标位图化成三张 PNG，矢量源改完跑一次即同步

### 变更

- **favicon 重做**：弃英文 G，改“渐”字加渐变圆角底；单一图形在 16 像素更认得出，也与中文品牌名对上；顶栏品牌块与图标共用同一份矢量与渐变色值，结束两套色值并存
- **历史挪位**：“最近生成”由面板列尾部移到列顶独立区域，不再与四个调节面板混排，桌面与手机打开即见
- **环境光默认压制**：浅色主题光晕颜色饱和乘 0.55、明度压一档，pastel 配色当背景不再晃眼；深色主题保持原样

### 验证

- 新增端到端：关于对话框版本号与恢复默认回退、环境光滑杆刷新留存；新增单测覆盖光晕压制与强度夹取
- `npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`、`npm run budget` 全绿
- `npx playwright test` 全绿；`npm run screenshots` 覆盖桌面、iPhone 15 与 iPhone SE 的深浅主题

## [3.2.1] - 2026-08-31

### 新增

- **撤销与重做**：顶栏两颗按钮，键盘 `Ctrl/Cmd+Z` 与 `Ctrl/Cmd+Shift+Z`，焦点在输入框时不拦截；上限 50 步，不进分享链接与存档
- **历史真缩略图**：历史条目异步渲染 96 px JPEG 缩略图，同配色不同质感的两条从此认得出来；旧存档没有缩略图时回落配色渐变加首字
- **noscript 提示**：禁用 JavaScript 的环境给出双语提示，不再一片空白
- **样张脚本**：`npm run samples` 用当轮默认值渲染质感 × 配色网格与文字效果样张，README 三张图一条命令重生成

### 变更

- 首屏 JS 预算统一为 250 KB gzip，`scripts/check-budget.mjs` 在 CI 构建后守卫；本轮实测 201.67 KB
- CI 增加独立 e2e job，push 到 main 与手动触发时跑，兑现 v3 规约的冒烟承诺
- 镜像 CSS 用 fontsource 固定版本替代 `@latest`，没有 npm 包的字体才回落 latest
- 去掉两份重复实现：`App.tsx` 改读契约的语区默认字体，store 改读引擎的随机种子
- 项目记忆目录由 `docs/claude-memory/` 改为 `docs/memory/`，不再绑定单一工具

### 修复

- `SECURITY.md` 重写到 v3 实际安全面；`.mcp.json` 改走本地 shadcn 二进制
- 补打 v3 线四个发布标签（v3.0.0 到 v3.1.2），发布约定写进 contributing

### 验证

- `npm run lint`、`npm run typecheck`、`npm test`（667 条）、`npm run build`、`npm run budget` 全绿
- `npx playwright test` 19 条全绿，含 noscript、撤销重做与历史缩略图相关用例

### 说明

- 本轮是 `docs/audits/2026-08-31-tech-doc-debt-audit.md` 与 `specs/v3.1.3-debt-and-hygiene/` 的全部落地；因 v3.2.0 已先发布，编号为 3.2.1 而非原计划的 3.1.3

## [3.2.0] - 2026-08-31

### 新增

- **图标徽章**：新增“图形在上、文字在下”的用途；图形比例 30% 到 80% 可调，文字在剩余安全区内自动填满
- **内置图标**：收录 lucide-react 1.37 的 1790 个主图标，精选 162 个带中英文关键词与类目；全库只在搜索需要时加载
- **Emoji 图形**：收录 emojibase-data 15.0.0 的 1879 个可分组 emoji，五种界面语言各有名称与标签；图形按码点取 Noto Emoji v2.047 的单个 SVG，跨设备一致
- **上传图形**：支持 SVG、PNG、WebP；SVG 用元素与属性白名单重建，剥掉脚本、事件属性、`foreignObject` 与外部引用；只在本次会话有效
- **图形选择器**：桌面命令对话框、手机底部抽屉；内置图标与 emoji 分栏搜索，上传动作固定在底部

### 变更

- `layout` 契约新增 `graphic` 与 `icon`；随图标徽章移除已废弃的 `layout.scale`，旧状态徽章链接在归一化时迁移到第二行行级字号
- URL 分享只保留内置图标与 emoji；上传图形降级为空来源
- 首屏 JS 预算统一为 250 KB gzip，实测 200.22 KB；图形、emoji 索引与选择器均不在首屏 chunk

### 验证

- 新增 SVG 消毒、emoji 文件名推导、索引结构、图形绘制消费端、图标排版与 URL 兼容单测
- e2e 覆盖内置图标、emoji、上传 SVG 与手机底部抽屉；桌面与 iPhone SE 另有图标徽章截图

## [3.1.2] - 2026-08-31

### 变更

- **默认文字参数**：边距改为 15%，行高改为 1.03；画布默认保持方形
- **质感面板**：种子输入、哈希、复制与换种子整体前置到质感选择之前
- **颗粒质感**：随机形状池去掉 ripple，不再出现同心圆；保留 wave 与 corners
- **行级字号**：控件前置到全局字号之前，数值框常驻可见，可直接输入百分比

### 验证

- `npm run lint`、`npm run typecheck`、`npm test`、`npm run build`、`npm run e2e` 全绿
- `npm run screenshots` 覆盖桌面、iPhone 15 与 iPhone SE 的深浅主题

## [3.1.1] - 2026-08-29

### 新增

- **行级排版**：纯文字模式可为每一行单独调字号比例（0.2 到 2 倍）与水平视觉补偿（±25% 画布宽度）；自动填满按各行实际包络求解，描边、投影、发光也按各自字号计算尺度
- **复制图片**：导出抽屉新增 PNG 剪贴板动作，`ClipboardItem` 接收 Promise，保证 Safari 的用户手势语义；不支持时明确提示改用下载

### 变更

- **默认画布**改为方形，不再默认圆角
- **默认示例**改为两行「飞书 / 效率先锋」，第二行字号为首行 0.62 倍
- **默认文字颜色**改为白色；自动取色保留为显式选项
- **主“导出”按钮**直接触发浏览器下载；导出选项通过旁边的设置图标打开，动作区固定为“下载”和“复制图片”

### 修复

- 旧状态徽章链接里的 `layout.scale` 会迁移到第二行行级字号，不再因为新契约而回到默认比例

## [3.1.0] - 2026-08-29

### 新增

- **状态徽章用途**：首行大字写状态、次行小字写日期，两块垂直堆叠居中，整体一起做自动填满。次行字号是首行的 0.42 倍，可在 0.2 到 0.8 之间调。适合请假、休假、出差这类要一批人统一出图的场景
- **逐行字号**：绘制改成按字号分段落笔，描边、发光、投影的尺度按各段自己的字号算，次行不会被首行的光圈糊掉
- **用途分段控件**：文字面板顶部选用途，状态徽章下文字输入换成状态与补充两个单行框，并隐藏锚点、对齐、竖排、自动换行四个在这个版式下不生效的控件

### 变更

- **默认示例文字**改为「飞书效率先锋」，**默认文字效果**改为发光
- `config` 新增 `layout` 子树（`kind` 与 `scale`），缺省即纯文字用途。v3 时代的分享链接原样打开仍是纯文字，不失效

### 修复

- 状态徽章的次行此前按首行字号落笔，画面里直接冲出画布。`LayoutLine.font` 加进契约后没有被 `drawText` 读取，排版对而绘制错

### 说明

- 规约里的图标徽章、内置 SVG 图标库与 emoji 推迟到 3.2。那一档要先有图形来源才成立，只落排版内核会在主干留下一块画不出东西的预留区。设计已写在 `specs/v3.1-badge-templates/`，代码未写

## [3.0.0] - 2026-08-29

### 新增

- **WebGL 渲染引擎**：四种质感（柔光、流动、丝绸、颗粒）由 WebGL2 fragment shader 实时渲染，同一配置在同一设备上像素级一致
- **26 套内置配色**：归 15 个家族，浅色深色各成一组，配色名带五种语言
- **配色生成**：自定义 2 到 6 个颜色，或给一两个种子色由 OKLCH 算法排出整套停靠点
- **文字排版**：自动填满与手动字号两种模式，行高、字间距、字重、对齐、九宫格锚点、偏移、竖排、自动换行全部可调
- **形状感知的文字安全区**：安全框先按边距内缩，再按画布遮罩几何收，圆形与大圆角下文字块四角不会被裁掉
- **五种文字效果**：纯色、描边、投影、发光、胶囊底，强度可调
- **自动文字色与自动底板**：内置配色用设计值，自定义配色按像素判定；两个候选色都到不了对比度门槛时自动补一层胶囊底板，不写回用户配置
- **Google Fonts 动态加载**：全库按需加载，中文字体按 unicode-range 切片只拉用到的字，超时后依次切两个 jsDelivr 镜像
- **本地字体上传**：TTF、OTF、WOFF、WOFF2 用 `FontFace` 直接注册，只在本次会话有效
- **导出目标体积**：JPG 与 WebP 支持 1 MB、2 MB 两档，用质量二分逼近目标，达不到时提示降分辨率
- **Web Share**：移动端导出优先进系统分享面板，可直接分享到聊天应用；微信内置浏览器改为提示长按图片保存
- **链接分享与历史**：全部参数编码进 URL hash，复制链接即可复现；本地保留最近 8 次配置。链接在传输中被截断时明确提示，不会静默按本机配置打开
- **手机端布局**：预览 sticky 在顶栏下，参数面板分段切换，操作条固定在屏幕底并让出 safe-area，导出与字体选择走底部抽屉
- **PWA**：可安装到桌面，离线时界面可用
- **端到端测试与截图脚本**：Playwright 分桌面 1440 与 iPhone 15 两组，`npm run screenshots` 覆盖三个设备的深浅主题

### 变更

- **整体重写**：从无框架的原生 DOM 换成 Vite 8、React 19、TypeScript strict、Tailwind CSS v4、shadcn/ui 与 zustand
- **渲染路线**：SVG 多层径向渐变换成 WebGL2 fragment shader，没有 WebGL2 的浏览器拿到静态近似图，预览与导出都用这张图
- **导出尺寸**：上限提到 8192，超过设备的 WebGL 与 2D 画布上限时按上限渲染后放大，导出面板显示本机最高原生边长
- **默认文字效果**：从纯色改为发光，深色字配白光、浅色字配同色光，任何配色下文字与背景都分得开；对比度不够时的胶囊底兜底同时覆盖纯色与发光两档
- **界面语言**：五种语言的字典改为扁平点分 key 并由源语言类型约束，配色名、默认字体与默认示例文字都跟随语言
- **项目更名**：产品名改为渐变头像生成器，包名与仓库名改为 `gradient-avatar`
- **运行环境**：Node 版本要求提到 24

### 移除

- **SVG 导出**：shader 渲染无法矢量化，头像本来就是位图用途
- **命令行工具与批量生成**：浏览器外没有 WebGL，需要时可用 Playwright 驱动页面另立脚本
- **内置阿里字体与 opentype.js 轮廓化**：由 Google Fonts 动态加载与 `FontFace` 取代，仓库里不再有字体文件
- **场景预设**：参数直接调，不再按场景预置整组配置
- **相关依赖**：`@resvg/resvg-js`、`commander`、`seedrandom`、`opentype.js`、`wawoff2` 全部移除

---

## [2.3.0] - 2026-03-22

### 新增

- **JPG 导出**：新增 JPG 下载功能，使用二分查找自适应压缩算法，自动将文件压缩至 ≤2MB（quality 区间 0.75-0.92）
- **移动端 FAB**：≤860px 屏幕下，下载按钮从导航栏移至右下角悬浮按钮组（FAB），支持 SVG/PNG/JPG 三种格式
- **噪点档位升级**：删除效果弱的"低"和"中"档位，新增"强烈"（baseFrequency=1.0）和"极致"（baseFrequency=1.5）档位

### 变更

- **噪点默认关闭**：噪点纹理默认值从"中"改为"关闭"，避免 PNG 体积暴涨
- **numOctaves 联动**：噪点 `numOctaves` 随 `baseFrequency` 自动联动（高=3, 强烈=4, 极致=5），高档位层次感更丰富
- **导出管线重构**：抽取 `renderToCanvas()` 公共函数，PNG/JPG 共享 SVG→Canvas 渲染管线，消除代码重复
- **导航栏优化**：移动端下载按钮移至 FAB 后，导航栏更简洁；≤380px 极小屏隐藏品牌文字仅保留 logo

### 无障碍

- FAB 完整 WCAG 2.2 支持：`aria-expanded`、`aria-haspopup="menu"`、`role="menu/menuitem"`、Escape 关闭 + 焦点回归

---

## [2.2.0] - 2026-03-21

### 工程化

- **Node.js 24 LTS**：从 EOL 的 Node 18 升级到最新 LTS（v24 Krypton）
- **ESLint 10 + Prettier**：新增代码规范工具链（flat config），`npm run lint` / `npm run format`
- **GitHub Actions CI**：每次 push 自动运行 lint + test
- **EditorConfig**：统一编辑器基础规范

### 架构重构

- **State Proxy 化**：`state.js` 改用 `Proxy` 实现变更追踪，支持 `onChange` 监听
- **事件绑定抽象**：提取 `bindOptionGroup()` 通用函数，消除 4 处重复的 click delegation 模式
- **Debounce**：所有 slider/textarea 输入节流 150ms，大幅减少 SVG 重建频率
- **Magic numbers 语义化**：`PILL_DEFAULTS`、`VERTICAL_COL_SPACING_RATIO`、`NOISE_FREQUENCY_MAP` 等
- **全局错误兜底**：新增 `unhandledrejection` handler

### i18n 完善

- **字典拆分**：572 行内联字典拆为 5 个独立 JSON 文件（`web/i18n/*.json`），Vite 自动 code-split
- **硬编码清理**：8 处硬编码中文替换为 `t()` 调用
- **新增 4 个 i18n key**：`app.title`、`placeholder.default_name`、`msg.svg_parse_error`、`file.default_name`
- **HTML lang 动态更新**：切换语言时同步更新 `<html lang>` 和 `<title>`
- **optgroup/aria-label i18n 支持**：`applyTranslations()` 新增 `data-i18n-label`、`data-i18n-aria-label`

### 可访问性 (a11y)

- **ARIA 标注**：Tab 组件 `role="tablist/tab/tabpanel"` + `aria-selected`
- **键盘导航**：Tab 支持方向键切换，自定义控件（palette/style/noise/resolution）添加 `tabindex` + `role="radio"`
- **表单关联**：slider/select 添加 `<label for="">` 关联
- **对比度修复**：暗色主题 `--text-3` 从 `#68687a` 提升到 `#8888a0`（WCAG AA 达标）
- **Focus 可见性**：全局 `:focus-visible` 样式
- **Icon button aria-label**：所有 icon button 添加可访问名称

### 导出修复

- **PNG 导出**：新增 `img.onerror` handler + `toBlob` 失败保护 + ObjectURL 内存泄漏修复
- **默认文件名 i18n 化**：导出文件名随语言切换

### 响应式

- **新增 520px 手机断点**：优化小屏布局、触摸目标 44px 最小尺寸
- 调整 tablet 断点下的 preview-frame 尺寸

### 其他

- **HTML meta 完善**：`theme-color`、`description`、SVG emoji favicon
- **WASM 本地化**：wawoff2 从 unpkg CDN 迁移到本地 `/wawoff2-decompress.js`
- **SVG 快照测试**：4 个 snapshot 测试用例，保护渐变算法稳定性
- **版本号更新**：header tag 从 v2.0 更新为 v2.1
- 测试从 204 增加到 208（含快照测试）

---

## [2.0.0] - 2026-03-21

### 新增

- **场景预设系统**：15+ 场景预设（电商、社交媒体、设计、壁纸），类似 Illustrator 新建画板
- **分辨率预设**：standard(1K) / HD / 2K / 4K / 8K 五档分辨率
- **矩形画布**：支持任意 width/height 宽高比，不再限于正方形
- **4 套柔和配色**：peach(桃粉) / mint(薄荷) / aurora(极光) / blush(腮红)，浅色基底
- **纯渐变模式**：textStyle: 'none' 生成无文字的纯渐变背景
- CLI 新增 `--preset`、`--resolution`、`--width`、`--height`、`--list-presets` 选项

### 构建与部署

- **Vite 构建工具**：引入 Vite 打包，解决浏览器 bare specifier 导入问题
- **Cloudflare Pages 部署支持**：`_headers`（安全头 + 缓存策略）、`_redirects`（SPA fallback）
- **Web 代码拆分**：将 767 行单文件 `index.html` 拆分为 `index.html` + `style.css` + `main.js`
- **新增开发命令**：`npm run dev`（Vite 热更新）、`npm run build`、`npm run preview`
- Node 版本锁定（`.node-version`）

### 变更

- 默认渐变层数从 12 调整为 8（更清晰的色块分离）
- 默认模糊从 0 调整为 3（更柔和的边缘过渡）
- 渐变焦点 fy 从固定 0.5 改为随机化（0.1-0.9），色彩分布更丰富
- 渐变缩放范围从 0.7-1.5 扩大到 0.5-2.0（更大胆的色块变化）
- Web 预览从 Python HTTP server 迁移至 Vite dev server
- 移除了项目中所有第三方公司品牌引用

---

## [1.0.0] - 2026-03-21

### 新增

- **核心渐变引擎**：基于 SVG 多层径向渐变叠加技术
  - 12 层 `<radialGradient>` 配合 transform 变换链
  - `filter:saturate()` 饱和度增强
  - 可选 `<feGaussianBlur>` 和 `<feTurbulence>` 滤镜
- **OKLCH 颜色科学**：使用 culori 库实现感知均匀的颜色插值
- **8 套预设配色方案**：warm / cool / sunset / forest / ocean / creative / tech / elegant
- **4 种文字排版**：胶囊标签 / 大字叠加 / 竖排 / 拆字艺术
- **命令行工具**：支持单条生成和批量生成（JSON/CSV/TXT 输入）
- **PNG 导出**：通过 @resvg/resvg-js，自动检测系统中文字体
- **总览 HTML**：批量生成后自动创建 overview.html
- **交互式 Web 预览**：实时调参、配色切换、SVG/PNG 下载
- **可复现性**：种子化随机数保证相同输入产生相同输出
- **XML 安全**：所有文本内容经过转义处理
- **204 个单元测试**：覆盖核心功能和边界情况
