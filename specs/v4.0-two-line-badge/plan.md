# 两行徽章模型 v4.0 计划（怎么造）

对应 `spec.md`。状态：待评审。切片 A 无依赖可先合，B 在 C 前，D 收尾。

## 切片 A：默认值与文件名（无依赖，先合）

- `DEFAULT_CONFIG` 效果改投影、强度 0.4；`draw.ts` 投影色按文字色反色适配。
- `ColorField` 增加四档预设色板与自选。
- `buildFilename` 加秒级时间戳（本地时区），`tests/export/filename.test.ts` 全部用例同步改写。
- 验收：黑字投影在深背景可见；同内容连导两次文件名不重复。

## 切片 B：栈求解器与契约迁移（核心）

- 新增栈求解器：合并 `fitStatus` 与 `layoutBadge`，支持图标＋两行的统一二分；
  `layoutText` 只剩这一条路，`layoutPlain` 与用途分派移除。
- 行级补偿解耦在新求解器内落地：先写回归用例（改一行、验其余行像素不动）令其红，再实现令其绿。
  旧 `composeHorizontal` 的 minOffset 归一化随旧求解器一并删除，不做过渡修补。
- 契约升 `v:4`：`normalizeConfig` 迁移层（三行并两行、用途映射、行级数组截断），
  URL 解码与存档共用入口；`layout.kind` 保留解析、停止分派。
- 验收：五种栈组合单测全绿；三行与各旧用途链接迁移用例全绿。

## 切片 C：界面重构

- `TextPanel` 重做：移除用途分段，第一行／第二行单行输入，图标开关常驻；
  排版组按 §3.5 保留清单裁剪。
- 常驻操作条：两个随机提为一级按钮，新增文字快捷入口（切页签＋聚焦第一行），窄屏纯图标态。
- 五语字典与 `keys.md` 同步；用途相关词条删除。
- 验收：手机与桌面断点下常驻条不溢出；快捷入口一步聚焦。

## 切片 D：收尾

- e2e 补旧链接兼容、常驻按钮、文件名时间戳三类用例。
- `npm run screenshots` 三机深浅主题全量对照。
- README、architecture、CHANGELOG、engineering-lessons 与项目记忆同轮更新；
  「三用途收敛为一栈」的决策如需要可补 ADR 0006。
- 发 4.0.0 并打标签。

## 风险与对策

- 迁移破坏存量链接：三种旧用途各一条迁移用例钉死，URL 与存档共用同一入口，不留第二条路。
- 砍掉锚点与竖排引发存量用户不适：属有意决策，写进 CHANGELOG 的破坏性变更段，不静默。
- 常驻条在 320px 排不开：延续现行「允许收缩、文案 truncate」策略，窄屏退纯图标。
- 切片 B 期间旧补偿 bug 仍可见：不单独修旧求解器，回归用例随 B 落地，避免白修。
